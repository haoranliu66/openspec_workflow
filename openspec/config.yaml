schema: product-change

context: |
  Shared BR, PRD, and FEATURE records live in docs/requirements/REQ-<id>-<slug>/ and can govern multiple OpenSpec changes. SPEC.md is generated navigation; pathless openspec/change-history.json v1 retains archived Requirement evolution without artifact paths. Current behavior lives in openspec/specs/<capability>/spec.md, while a change only owns its deltas under openspec/changes/<change>/specs/<capability>/spec.md.
  Select bugfix only to restore confirmed behavior without a new Requirement. Select the
  system-change governance path by writing schema: spec-driven for bounded observable system
  behavior that changes no product goal, user journey, role permission, business rule, public
  contract, or shared product acceptance. Select product-change for every product-governed
  change regardless of implementation size. OpenSpec 1.5.0 supplies spec-driven; this project
  does not copy that built-in Schema.
  For code, product-behavior, or system-behavior changes, this workflow is the project's sole
  change-delivery lifecycle. Other workflows and skills may be loaded only when the user, platform,
  or current task requires them, and then only as bounded helpers inside the active change. They must
  not create a parallel delivery lifecycle or bypass artifacts and change-scoped authorization.
  Higher-priority instructions and mandatory project security, release, migration, operations, and
  domain rules remain effective. AI must report material conflicts with their source, impact,
  priority, and proposed resolution, and stop for user direction when the conflict cannot be resolved.
  GitNexus is recommended but optional for large or unfamiliar repositories. Check repository context
  and index freshness first, select only the task-matched capability, and fall back to code search and
  tests when unavailable. GitNexus results do not replace specs, code, tests, verification, team review,
  or any Schema, CI, or formal-close gate.
  The sole complete lifecycle is the Unified Lifecycle in docs/FULLSTACK_WORKFLOW.md: request intake,
  explore, path selection, planning, implementation authorization, implementation, verification and team
  review, close authorization, and formal close. Follow that section for the complete `/opsx:explore`
  contract and do not create a parallel lifecycle in this config. This config only adds OpenSpec-specific
  context and execution constraints.
  Completing shared BR/PRD before starting a product change is team process governance.
  It is not enforced by the schema, scripts, or CI; proposal remains an independent
  root in the native OpenSpec artifact graph. Archived changes are treated as immutable
  by team process, without requiring programmatic or CI enforcement.
  Artifact readiness is not implementation authorization. After the Schema-aware apply-required
  artifact dependency closure is complete or materially changed, AI must end the current turn,
  show the exact change ID and plan summary, and wait for explicit authorization in a later message.
  Authorization is change-scoped, is invalidated by material planning changes, and is team process
  governance rather than an approval artifact or Schema/script/CI proof.
  After implementation, keep detailed test cases, actual results, risk applicability, incomplete work,
  limitations, rollback notes, and safe evidence references in openspec/changes/<change>/verification.md
  with optional sibling evidence/. This record and the team's completion judgment are governance, not
  OpenSpec artifacts or Schema/script/CI gates. Before AI runs formal close, it must present the exact
  change and verification summary, end the turn, and wait for later explicit close authorization.
  This upstream workflow source repository excludes its own numbered REQ packages, detailed changes,
  archives, verification, evidence, and ad-hoc artifacts from the public tracked checkout after their
  summaries are captured. The installer does not copy that Git policy; downstream repositories own
  their retention policy.
rules:
  br:
    - Link the single shared BR instead of copying it into a change.
    - Record only the target ID, delivery slice, evidence status, and blockers.
  prd:
    - Link the shared PRD and identify the independently deliverable slice.
    - Keep outcome-level acceptance here; executable WHEN/THEN scenarios belong only in specs.
  proposal:
    - For product-change only, link the governing BR and PRD while keeping the proposal independently releasable.
    - For product-change only, link the governing BR and PRD when the team process prerequisite has been completed; this prerequisite is not a schema or CI gate.
    - Use the native Why, What Changes, Capabilities, and Impact sections.
  specs:
    - Use behavior-first SHALL/MUST requirements and precise WHEN/THEN scenarios.
    - Give every ADDED, MODIFIED, REMOVED, or RENAMED delta Requirement a stable ID.
    - For product-change only, team review traces every delta Requirement ID to shared PRD acceptance and truthful FEATURE conclusions.
    - Trace historical changes through the pathless history ledger; inspect a detailed local archive only when it exists and is needed, without modifying it.
  design:
    - design.md is the only technical design; trace decisions to requirement IDs.
  tasks:
    - Order numbered checkbox work by dependency and record independently verifiable evidence.
    - Before team close review, inspect every real checkbox. AI must explain delivery impact and affected Requirement/FEATURE claims for work it judges impossible, cancelled, or deferred.
    - Record applicable validation and delivery evidence in change-local verification.md; do not create closeout.json or treat verification headings as a machine contract.
    - Implementation authorization does not authorize close. Present the verification summary and wait for later explicit change-scoped close authorization.
